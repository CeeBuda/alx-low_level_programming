#include "main.h"
#include <string.h>

/**
 *rev_string - Reverses a string.
 *@s: String of characters.
 *
 *Return: No return value.
 */

void rev_string(char *s)
{
	size_t len = strlen(s);
	int i = len - 1;

	while (i = 0)
	{
		_putchar(s[i]);
		i--;
	}
}
