#include "main.h"

/**
 * _strpbrk - breaks or does something to a string
 * @s: string
 * @accept: substring
 *
 * Return: pointer to string
 */
char *_strpbrk(char *s, char *accept)
{
	return (s);
}
